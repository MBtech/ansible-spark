# Spark masters (as slaves) list
# {{ hostvars[groups['tag_ds_role_spark_master'][0]].private_ip }}

# Spark slaves list
{% for host in groups['tag_ds_role_spark_slave'] %}
{{ hostvars[host].private_ip }}
{% endfor %}
